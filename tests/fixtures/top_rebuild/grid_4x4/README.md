# grid_4x4 fixture

Synthetic 4×4 continuous `Tile_+x_+y` tileset for TopRebuild Phase 5.
16 leaf blocks, each with coarse + fine Representation stubs (empty .b3dm).
Expected quadtree: L0=16 → L1=4 → L2=1.
