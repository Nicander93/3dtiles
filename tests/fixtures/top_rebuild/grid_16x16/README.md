# grid_16x16 fixture

Synthetic 16×16 continuous `Tile_+x_+y` tileset for TopRebuild Phase 10 scale ladder.
256 leaf blocks, each with coarse + fine Representation stubs (empty .b3dm).
Expected quadtree: L0=256 → L1=64 → L2=16 → L3=4 → L4=1.
Cell size 100 m (same spacing as grid_4x4).
