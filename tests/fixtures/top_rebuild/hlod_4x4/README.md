# hlod_4x4 fixture (Phase 7)

Generated by:

```bash
cargo run -p top_rebuild --bin top_rebuild_debug -- rebuild \
  --input tests/fixtures/top_rebuild/grid_4x4 \
  --out tests/fixtures/top_rebuild/hlod_4x4 \
  --l1-max-triangles 800 --l2-max-triangles 1200 --segments 6
```

Expected hierarchy:

```text
16 Original Blocks (external Tile_* tilesets with synthesized B3DM)
      ↓ REPLACE
4 L1 Proxy (Proxy_L1_* .b3dm)
      ↓ REPLACE
1 L2 Root Proxy (Proxy_L2_0_0.b3dm)
```

Also mirrored at `/workspace/data/geoforge_outputs/phase7_hlod_4x4`.
World-space leaf meshes + parent-local proxy meshes; tile `transform` keeps world invariant.
Texture optimization OFF.
