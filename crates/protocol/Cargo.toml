[package]
name = "geoforge-protocol"
version = "0.1.0"
edition = "2021"
description = "GeoForge task config and event contract (no runtime I/O)"
authors = ["GeoForge"]
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
