/**
 * Phase 17 placeholder — Cesium A/B FPS / SSE / screenshot harness.
 * Phase 15 ships the file so the acceptance tree matches plan §8;
 * implement camera sets + metrics in Phase 17.
 *
 * Usage (future):
 *   node scripts/acceptance/cesium_benchmark.mjs --tileset <url> --out <dir>
 */
console.error(
  'cesium_benchmark.mjs: stub for Phase 17 — not executed as a Phase 15/16 gate.'
);
process.exit(0);
